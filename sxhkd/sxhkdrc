# screenshotting utility
Print
	scrot -q 80 -s -f -z -e 'xclip -selection clipboard -t image/png -i $f; rm -rf $f'

# media keys
XF86AudioPlay
	playerctl play-pause

XF86AudioNext
	playerctl next

XF86AudioPrev
	playerctl previous
